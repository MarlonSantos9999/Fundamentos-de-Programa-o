#include <stdio.h>

int main() {
    int X;        // distância (km)
    double Y;     // combustível (litros)
    double consumo;

    scanf("%d %lf", &X, &Y);

    consumo = X / Y;

    printf("%.3lf km/l\n", consumo);

    return 0;
}
