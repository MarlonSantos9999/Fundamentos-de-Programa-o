#include <stdio.h>

int main() {
    int distancia;
    int tempo;

    scanf("%d", &distancia);

    tempo = distancia * 2; // 1 km a cada 2 minutos

    printf("%d minutos\n", tempo);

    return 0;
}
